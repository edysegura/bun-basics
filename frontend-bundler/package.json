{
  "scripts": {
    "dev": "bun index.html",
    "prebuild": "rm -fr dist",
    "build": "bun build ./index.html --target=browser --minify --outdir ./dist",
    "test": "bun test",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@picocss/pico": "^2.1.1"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "typescript": "^5.3.0",
    "@types/jsdom": "^21.1.6"
  }
}