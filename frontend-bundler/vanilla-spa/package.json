{
  "scripts": {
    "dev": "bun ./src/index.html --console",
    "start": "bun ./dist/index.html",
    "prebuild": "rm -fr ./dist",
    "build": "bun build ./src/index.html --production --target=browser --minify --outdir ./dist"
  }
}